*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
*.apk
*.aab
output.json
letstrack-release.keystore
lets-track-keystore.jks
keystore.properties

# Merchant-data pipeline (datasets/) -- source PBF, generated intermediate outputs, and the
# Python venv are all regenerable/re-downloadable, not original work. Only the small pipeline
# scripts themselves are worth tracking. See datasets/extract.py's own doc comment for how to
# regenerate everything below if ever needed.
datasets/venv/
datasets/data/
datasets/output/*.csv
datasets/output/*.json
datasets/output/*.md
!datasets/output/common_merchants_curated.json
